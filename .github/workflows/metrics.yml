name: Build metrics

# ---------------------------------------------------------------------------
# Build-time & binary-size metrics (milestone 1.14).
# ---------------------------------------------------------------------------
# Runs alongside CI on every push / pull_request. Produces downloadable
# artifacts and a per-crate timing breakdown in the workflow run summary, but
# holds NO gating verdicts: every measurable step is wrapped in
# `continue-on-error: true` so a metrics miss cannot fail a PR. Gating
# decisions are deferred until a stable size/time baseline exists (see the
# gate-posture note appended to this file by task 4 of the milestone).
# ---------------------------------------------------------------------------

on: [push, pull_request]

jobs:
  metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      # -------------------------------------------------------------------
      # Timing report (task 1 of milestone 1.14).
      # -------------------------------------------------------------------
      # `cargo build --workspace --timings` writes the timing report to
      # `target/cargo-timings/cargo-timing-<timestamp>-<hash>.html` plus a
      # `cargo-timing.html` symlink pointing at the freshest entry. The
      # milestone subtask refers to it as `target/cargo-timings.html`, the
      # colloquial handle for that report; we upload the directory so both the
      # timestamped file and the `cargo-timing.html` pointer are downloaded.
      # The per-crate breakdown below extracts the embedded `UNIT_DATA` JSON
      # array from the freshest HTML and posts a Markdown table of total
      # duration per crate to `$GITHUB_STEP_SUMMARY`.
      # -------------------------------------------------------------------
      - name: cargo build --workspace --timings
        continue-on-error: true
        run: cargo build --workspace --timings

      - name: Upload cargo-timings artifact
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: timings
          path: target/cargo-timings/

      - name: Per-crate timing breakdown in $GITHUB_STEP_SUMMARY
        continue-on-error: true
        shell: bash
        run: |
          python3 - <<'PY'
          import json, os, re, sys
          from pathlib import Path

          candidates = sorted(
              Path("target/cargo-timings").glob("cargo-timing-*.html"),
              reverse=True,
          )
          lines = ["## Per-crate build timings", ""]

          if not candidates:
              lines += ["_cargo-timing-*.html not found under target/cargo-timings._", ""]
          else:
              html = candidates[0].read_text(encoding="utf-8")
              m = re.search(r"UNIT_DATA\s*=\s*(\[.*?\])\s*;", html, re.S)
              if not m:
                  lines += ["_UNIT_DATA not found in cargo-timings.html._", ""]
              else:
                  try:
                      units = json.loads(m.group(1))
                  except Exception as exc:
                      lines += [f"_UNIT_DATA parse failed: {exc}._", ""]
                      units = None

                  totals = {}
                  if isinstance(units, list):
                      for u in units:
                          if not isinstance(u, dict):
                              continue
                          name = u.get("name") or "?"
                          dur = u.get("duration")
                          if not isinstance(dur, (int, float)):
                              continue
                          totals[name] = totals.get(name, 0.0) + float(dur)

                  if totals:
                      lines += ["| Crate | Total duration (s) |", "|---|---|"]
                      for name, dur in sorted(
                          totals.items(), key=lambda kv: kv[1], reverse=True
                      ):
                          lines.append(f"| {name} | {dur:.3f} |")
                  else:
                      lines += ["_No per-unit durations in cargo-timings.html._", ""]

          text = "\n".join(lines)
          print(text)
          summary = os.environ.get("GITHUB_STEP_SUMMARY")
          if summary:
              with open(summary, "a", encoding="utf-8") as fh:
                  fh.write(text + "\n")
          PY

      # -------------------------------------------------------------------
      # Binary-size tracking (task 2 of milestone 1.14).
      # -------------------------------------------------------------------
      # The YMX CLI Cargo *package* `ymx-cli` builds a `[[bin]] name = "ymx"`
      # binary (see `crates/ymx-cli/Cargo.toml`), so the milestone prose's
      # `target/debug/ymx-cli` size shorthand resolves to `target/debug/ymx`
      # here. The earlier `cargo build --workspace --timings` step above
      # already produced the debug binary at `target/debug/ymx`. This section
      # additionally builds a release-stripped variant and writes both byte
      # totals to `binary-size.json` (uploaded per build; no historical trend
      # store until a remote / trend backend exists).
      # -------------------------------------------------------------------
      - name: cargo build --release -p ymx-cli
        continue-on-error: true
        run: cargo build --release -p ymx-cli

      - name: strip release binary
        continue-on-error: true
        run: strip target/release/ymx

      - name: Capture binary sizes -> binary-size.json
        continue-on-error: true
        shell: bash
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path

          out = {}
          dbg = Path("target/debug/ymx")
          if dbg.exists():
              out["debug"] = {"path": str(dbg), "bytes": dbg.stat().st_size}
          else:
              out["debug"] = {
                  "path": str(dbg),
                  "bytes": None,
                  "note": "target/debug/ymx not present; build step may have failed",
              }
          rel = Path("target/release/ymx")
          if rel.exists():
              out["release_stripped"] = {"path": str(rel), "bytes": rel.stat().st_size}
          else:
              out["release_stripped"] = {
                  "path": str(rel),
                  "bytes": None,
                  "note": "target/release/ymx not present; release build or strip step may have failed",
              }
          text = json.dumps(out, indent=2, sort_keys=True) + "\n"
          Path("binary-size.json").write_text(text)
          print(text)
          PY

      - name: Upload binary-size artifact
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: binary-size
          path: binary-size.json

      # -------------------------------------------------------------------
      # (Optional) Cyclomatic-complexity export (task 3 of milestone 1.14).
      # -------------------------------------------------------------------
      # Decision (subtask "Decide whether to surface `metrics.yml` as a
      # separate workflow vs. fold into `ci.yml`"): metrics concerns live in
      # this dedicated workflow, NOT folded into `ci.yml`. Rationale:
      # `ci.yml` is the narrow fmt/clippy/test gate and must not gain
      # metrics-only side effects; isolation across audits (`audit.yml`),
      # coverage (`coverage.yml`), and metrics (this file) is the established
      # repo precedent and keeps each workflow's trigger / caching / failure
      # semantics independent.
      #
      # The `rust-code-analysis-action` invocation below is left commented
      # pending validation against the project's `rust-toolchain.toml` pin
      # (`rust-code-analysis` requires per-toolchain compatibility) and
      # confirmation of the PMD-style JSON import path on the trending
      # backend. Uncomment the two steps below once that validation is done;
      # the resulting `rust-code-analysis-pmd.json` is the PMD-complexity
      # artifact for trending on `main`.
      # -------------------------------------------------------------------
      # - name: rust-code-analysis complexity export
      #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      #   continue-on-error: true
      #   uses: softdevteam/rust-code-analysis-action@v0.0.7
      #   with:
      #     input-path: crates/
      #     output-file: rust-code-analysis-pmd.json
      #     output-type: pmd
      #
      # - name: Upload rust-code-analysis artifact
      #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      #   continue-on-error: true
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: rust-code-analysis-pmd
      #     path: rust-code-analysis-pmd.json

      # -------------------------------------------------------------------
      # Gate posture (task 4 of milestone 1.14) — informational-only.
      # -------------------------------------------------------------------
      # Every measurable step in this workflow is wrapped in
      # `continue-on-error: true` (above) so that miss/instability in the
      # timing report, the binary-size capture, or the rust-code-analysis
      # export cannot fail a PR. This workflow publishes only descriptive
      # artifacts and a Markdown summary; no budgets are enforced.
      #
      # Follow-up (once a stable baseline exists): introduce explicit size &
      # time budgets here — promote the relevant metric step's
      # `continue-on-error: true` to a hard failure when its measured value
      # crosses the budget (rust-code-analysis-action output,
      # `binary-size.json` byte totals, or per-crate timing totals). Baseline
      # the budgets against the most-recent N green runs (median or p95),
      # not aspirational targets; gate behind the existing `ci.yml`
      # fmt/clippy/test gate so repo policy stays in one place.
      # -------------------------------------------------------------------