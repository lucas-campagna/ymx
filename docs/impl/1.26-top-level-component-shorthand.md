---
version: "1.26"
title: "Fix top-level `$` component-name modifier"
short: "Top-level `sum$: x + y` ≡ `sum: ${x + y}`"
description: |
  Implement the top-level component-name `$` modifier that was documented in
  milestone 1.18 but not fully implemented. Strip `$` from top-level keys
  before validating the component name, so `sum$: a + b` is stored as component
  `sum` with body `${a + b}`. Also covers: `?$` combination on top-level keys,
  and `$_test`/`$_ymx` top-level reject (E010).
status: planned
depends_on: ["1.6"]
tags: [resolver, core, rules]
---

# 1.26 — Fix top-level `$` component-name modifier

## Tasks

- [x] Strip `$` from top-level key before component-name validation
- [x] Top-level `a$: x + y` → component `a` with math body `${x + y}`
- [x] Top-level `?$` combination (`a?$: x + y`) → optional with math-evaluated default
- [x] Top-level `$_test`/`$_ymx` with `$` → E010 (already handled)
- [x] Scenario `tests/cases/rule-18/component-name-shorthand/` updated to cover top-level case

## Acceptance
- [x] Scenario passes
- [x] `cargo test --workspace` passes
- [x] `cargo fmt --check && cargo clippy --workspace --all-targets` passes clean
