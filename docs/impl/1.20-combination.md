---
version: "1.20"
title: "Rule 17+18: `?$` optional property with math-evaluated default"
short: "Optional key with math-shorthand default value"
description: |
  Implement the `?$` combination: `<name>?$: <src>` ≡ `<name>?: ${<src}>` —
  an optional property whose default is a math-evaluated expression. When the
  caller does not supply `<name>`, the math source is evaluated lazily and the
  result is used as the default. When caller supplies `<name>`, the default is
  skipped entirely (never evaluated — lazy semantics). Works on named properties
  and integer positional keys (`0?$:` ⇒ optional slot with math default).
status: planned
depends_on: ["1.18", "1.19"]
tags: [resolver, core, rules]
---

# 1.20 — Rule 17+18: `?$` optional property with math-evaluated default

## Tasks

[x] Strip `?$` from key name; record as optional named property with math-evaluated default
[x] `0?$:` etc. — optional positional slot with math-evaluated default
[x] Lazy evaluation: default is only evaluated when caller is absent
[x] `?$` on meta fields `_ymx`/`_test` → E010
[x] Wrong order `$?:` → E010 (unchanged)

## Acceptance
[x] Scenario `tests/cases/rule-20/` passes
[x] `cargo test --workspace` passes
[x] `cargo fmt --check && cargo clippy --workspace --all-targets` passes clean
