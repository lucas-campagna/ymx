---
version: "1.34"
title: "Additional builtins: string / array / object / type / conditional helpers"
short: "Full builtin wave: $split, $join, $trim, $upper, $lower, $replace, $filter, $sort, $reverse, $unique, $flatten, $first, $last, $slice, $keys, $values, $entries, $from_entries, $pick, $omit, $type, $is_array, $is_object, $is_string, $is_number, $is_null, $to_string, $to_number, $coalesce, $sum, $avg, $min, $max, $if, $when"
status: in-progress
depends_on: ["1.8"]
tags: [builtins]
---

## Tasks

### String builtins (all eager args)

- [x] `$split(str, delim)` — str and delim both String → Array<string>; delim empty string → E011
- [x] `$join(arr, delim)` — arr must be Array, delim must be String → string; empty array → empty string
- [x] `$trim(str)` — str must be String → string with leading/trailing whitespace removed
- [x] `$upper(str)` — str must be String → uppercase string
- [x] `$lower(str)` — str must be String → lowercase string
- [x] `$replace(str, pattern, replacement)` — all args String; replaces all occurrences of pattern with replacement (no regex); result is string

   All string-builtin args (str, delim, pattern, replacement) support passing a component
   reference `$name` in addition to literal values; the reference is resolved through the
   caller's scope before use.

### Array builtins (all eager args, arr must be Array unless noted)

- [x] `$filter(fn, arr)` — first arg unevaluated callable (like $map/$reduce), second arg eager Array; keeps items where fn returns truthy; truthy = Bool(true) or non-Null/non-false non-bool; empty → empty array; array item → E011 (same as $map)
- [x] `$sort(arr)` — arr must be Array of numbers or strings; returns sorted copy, stable order; non-homogeneous or non-comparable → E011
- [x] `$reverse(arr)` — arr must be Array; returns reversed copy
- [x] `$unique(arr)` — arr must be Array; returns new array with first occurrence of each value kept (equality via Value::eq); preserves order
- [x] `$flatten(arr)` — arr must be Array; one level of flattening: each array element is spliced in-place, non-array elements remain
- [x] `$first(arr)` — arr must be Array → first element or Null if empty
- [x] `$last(arr)` — arr must be Array → last element or Null if empty
- [x] `$slice(arr, start, end)` — arr must be Array, start and end are Int; end is exclusive; negative indices count from end; clamped to bounds; returns subarray

   The arr argument supports passing a component reference `$name` in addition to a literal
   array; it is resolved through the caller's scope. Builtins with callable args ($filter, $when)
   additionally accept a bare component name as the callable.

### Object builtins (all eager args, obj must be Object unless noted)

- [x] `$keys(obj)` — obj must be Object → Array<string> (preserves key insertion order)
- [x] `$values(obj)` — obj must be Object → Array (preserves value order matching keys)
- [x] `$entries(obj)` — obj must be Object → Array<Object> where each entry is `{key: <k>, value: <v>}`
- [x] `$from_entries(arr)` — arr must be Array of objects each with `key` (string) and `value` fields; returns Object assembled from entries (later entries overwrite earlier on duplicate keys)
- [x] `$pick(obj, keys)` — obj must be Object, keys must be Array<string>; returns new object with only the listed keys (key not present → skipped)
- [x] `$omit(obj, keys)` — obj must be Object, keys must be Array<string>; returns new object without the listed keys

   The obj argument supports passing a component reference `$name` in addition to a literal
   object; it is resolved through the caller's scope.

### Type checking / coercion (all eager args)

- [x] `$type(val)` — val any Value → string: `"null" | "bool" | "int" | "float" | "string" | "array" | "object"`
- [x] `$is_array(val)` — val any Value → bool
- [x] `$is_object(val)` — val any Value → bool
- [x] `$is_string(val)` — val any Value → bool
- [x] `$is_number(val)` — val any Value → bool (Int or Float)
- [x] `$is_null(val)` — val any Value → bool
- [x] `$to_string(val)` — val any Value → string (same rendering as interpolation, including numbers)
- [x] `$to_number(str)` — str must be String → parses numeric string → Int or Float, or Null if not parseable

   All type-builtin args support passing a component reference `$name` in addition to a
   literal value; the reference is resolved through the caller's scope.

### Aggregate math (all eager args, arr must be Array of numbers)

- [x] `$sum(arr)` — arr must be Array of numbers (Int/Float); empty → 0
- [x] `$avg(arr)` — arr must be Array of numbers; empty → Null
- [x] `$min(arr)` — arr must be Array of numbers; empty → Null
- [x] `$max(arr)` — arr must be Array of numbers; empty → Null

   The arr argument supports passing a component reference `$name` in addition to a literal
   array; it is resolved through the caller's scope.

### Conditional builtins

- [x] `$if(cond, then, else)` — cond must be Bool; returns then if true, else if false; only the selected branch is evaluated (lazy semantics); the unselected branch is never evaluated
- [x] `$if` property-call syntax: `if: <cond>\n  then: <then_val>\n  else: <else_val>` — only `if`/`then`/`else` property keys allowed; any other key → E010
- [x] `$when(fn, arr)` — first arg unevaluated callable, second arg eager Array; applies fn to each item and keeps it if result is truthy (same truthy rule as $filter); result is Array of kept (transformed) items

   The cond/then/else args support passing component references; then/else in $if are
   lazily evaluated (only the selected branch's component is invoked).

### Infrastructure

- [x] Update `Builtin::from_name` and `Builtin` enum to cover all 39 builtins
- [x] Reserved names: extend the load-time reserved-name check to cover all builtin effective identifiers (split, join, trim, upper, lower, replace, filter, sort, reverse, unique, flatten, first, last, slice, keys, values, entries, from_entries, pick, omit, type, is_array, is_object, is_string, is_number, is_null, to_string, to_number, coalesce, sum, avg, min, max, when)
- [ ] E011 type errors for wrong argument types on every builtin
- [ ] Unit tests for each builtin; E011 type-error cases

## Acceptance criteria

### String builtins
- [ ] `$split("a,b,c", ",")` → `["a","b","c"]`
- [ ] `$split("hello", "")` → `["h","e","l","l","o"]`
- [ ] `$split("a::b::c", "::")` → `["a","b","c"]`
- [ ] `$split("abc", ",")` → `["abc"]` (no match → single-element array)
- [ ] `$split("a,b", "")` → E011 (empty delimiter)
- [ ] `$join(["a","b","c"], "-")` → `"a-b-c"`
- [ ] `$join([], "x")` → `""`
- [ ] `$join(["hello"], "")` → `"hello"`
- [ ] `$trim("  hello  ")` → `"hello"`
- [ ] `$trim("hello")` → `"hello"` (no whitespace → unchanged)
- [ ] `$upper("hello")` → `"HELLO"`
- [ ] `$lower("HELLO")` → `"hello"`
- [ ] `$replace("hello world", "world", "rust")` → `"hello rust"`
- [ ] `$replace("aaaa", "a", "b")` → `"bbbb"` (all occurrences)
- [ ] `$replace("abc", "x", "y")` → `"abc"` (no match)

### Array builtins
- [ ] `$first([1,2,3])` → `1`
- [ ] `$first([])` → `Null`
- [ ] `$last([1,2,3])` → `3`
- [ ] `$last([])` → `Null`
- [ ] `$sort([3,1,2])` → `[1,2,3]`
- [ ] `$sort(["c","a","b"])` → `["a","b","c"]`
- [ ] `$sort([1,"a"])` → E011 (non-homogeneous)
- [ ] `$reverse([1,2,3])` → `[3,2,1]`
- [ ] `$unique([1,2,1,3,2])` → `[1,2,3]` (preserves first occurrence order)
- [ ] `$flatten([[1,2],[3,[4]]])` → `[1,2,3,[4]]` (one level only)
- [ ] `$flatten([1,[2],3])` → `[1,2,3]`
- [ ] `$slice([0,1,2,3,4], 1, 3)` → `[1,2]`
- [ ] `$slice([0,1,2,3,4], 2, -1)` → `[2,3]` (negative end)
- [ ] `$slice([0,1,2], 0, 99)` → `[0,1,2]` (clamped)
- [ ] `$filter($is_even, [1,2,3,4])` where `$is_even: "${0 % 2 == 0}"` → `[2,4]`
- [ ] `$filter` with empty array → `[]`
- [ ] `$filter` with array item → E011
- [ ] Unknown component via $filter callable → E002
- [ ] `$when($double, [1,2,3])` where `$double: "${0 * 2}"` → `[2,4,6]` (fn applied AND truthy filter combined)
- [ ] `$when` with array item → E011

### Object builtins
- [ ] `$keys({b:2,a:1})` → `["b","a"]` (preserves insertion order)
- [ ] `$values({b:2,a:1})` → `[2,1]`
- [ ] `$entries({b:2,a:1})` → `[{key:"b",value:2},{key:"a",value:1}]`
- [ ] `$pick({a:1,b:2,c:3}, ["a","c"])` → `{a:1,c:3}` (order preserved from obj)
- [ ] `$pick({a:1}, ["nonexistent"])` → `{a:1}` (key not found → skipped)
- [ ] `$omit({a:1,b:2,c:3}, ["b"])` → `{a:1,c:3}`
- [ ] `$from_entries([{key:"a",value:1},{key:"b",value:2}])` → `{a:1,b:2}`
- [ ] `$from_entries([])` → `{}` (empty object)
- [ ] `$from_entries([{key:"a",value:1},{key:"a",value:2}])` → `{a:2}` (later wins on duplicate key)
- [ ] `$from_entries([{key:1,value:2}])` → E011 (key must be string)
- [ ] `$from_entries([{wrong:"field"}])` → E011 (missing key field)
- [ ] `$from_entries("not an array")` → E011

### Type builtins
- [ ] `$type(null)` → `"null"`
- [ ] `$type(true)` → `"bool"`
- [ ] `$type(1)` → `"int"`
- [ ] `$type(1.5)` → `"float"`
- [ ] `$type("hello")` → `"string"`
- [ ] `$type([1,2])` → `"array"`
- [ ] `$type({a:1})` → `"object"`
- [ ] `$is_array([1,2])` → `true`
- [ ] `$is_array({a:1})` → `false`
- [ ] `$is_object({a:1})` → `true`
- [ ] `$is_object([1,2])` → `false`
- [ ] `$is_string("hello")` → `true`
- [ ] `$is_number(42)` → `true`
- [ ] `$is_number("42")` → `false`
- [ ] `$is_null(null)` → `true`
- [ ] `$is_null(false)` → `false`
- [ ] `$to_string(123)` → `"123"`
- [ ] `$to_string(2.5)` → `"2.5"` (not "2")
- [ ] `$to_string(null)` → `"null"`
- [ ] `$to_number("42")` → `42` (Int)
- [ ] `$to_number("3.14")` → `3.14` (Float)
- [ ] `$to_number("hello")` → `null` (not parseable)
- [ ] `$to_number(123)` → `123` (pass-through for numbers)
- [ ] `$coalesce(null, null, 1, 2)` → `1`
- [ ] `$coalesce(null, false, 0)` → `false` (false is not null, so returned)
- [ ] `$coalesce(null, null)` → `null`

### Math aggregate builtins
- [ ] `$sum([1,2,3])` → `6`
- [ ] `$sum([])` → `0`
- [ ] `$avg([1,2,3])` → `2.0`
- [ ] `$avg([])` → `null`
- [ ] `$min([3,1,2])` → `1`
- [ ] `$max([3,1,2])` → `3`
- [ ] `$min([])` → `null`
- [ ] `$max([])` → `null`
- [ ] `$sum(["a","b"])` → E011 (non-numeric)

### Conditional builtins
- [ ] `$if(true, "yes", "no")` → `"yes"`
- [ ] `$if(false, "yes", "no")` → `"no"`
- [ ] `$if(null, "yes", "no")` → `"no"` (only Bool(true) is truthy)
- [ ] `$if(0, "yes", "no")` → `"no"` (0 is not truthy)
- [ ] Lazy semantics — unselected branch is not evaluated; if the selected branch is a component call it is invoked, the other is skipped entirely
- [ ] `$if` property-call syntax: `if: true\n  then: "yes"\n  else: "no"` → `"yes"`
- [ ] `$if` property-call syntax: unknown key (e.g. `unless:`) → E010
- [ ] `$when($add_ten, [5, 0, -3])` where `$add_ten: "${0 + 10}"` → `[15, 10, 7]` (transform + filter falsy)
- [ ] `$when` empty array → `[]`
- [ ] `$when` with array item → E011

### Component reference args

These test that component references work as builtin arguments (not just literal values).

- [ ] `$split($my_str, $delim)` where `my_str: "a,b,c"` and `delim: ","` → `["a","b","c"]`
- [ ] `$join($my_arr, $sep)` where `my_arr: ["a","b"]` and `sep: "-"` → `"a-b"`
- [ ] `$trim($my_str)` where `my_str: "  hello  "` → `"hello"`
- [ ] `$upper($my_str)` where `my_str: "hello"` → `"HELLO"`
- [ ] `$lower($my_str)` where `my_str: "HELLO"` → `"hello"`
- [ ] `$replace($my_str, $old, $new)` where `my_str: "hello world"`, `old: "world"`, `new: "rust"` → `"hello rust"`
- [ ] `$first($my_arr)` where `my_arr: [1,2,3]` → `1`
- [ ] `$last($my_arr)` where `my_arr: [1,2,3]` → `3`
- [ ] `$sort($my_arr)` where `my_arr: [3,1,2]` → `[1,2,3]`
- [ ] `$reverse($my_arr)` where `my_arr: [1,2,3]` → `[3,2,1]`
- [ ] `$unique($my_arr)` where `my_arr: [1,2,1,3]` → `[1,2,3]`
- [ ] `$flatten($my_arr)` where `my_arr: [[1,2],[3]]` → `[1,2,3]`
- [ ] `$slice($my_arr, 1, 3)` where `my_arr: [0,1,2,3,4]` → `[1,2]`
- [ ] `$filter($is_even, $nums)` where `is_even: "${0 % 2 == 0}"` and `nums: [1,2,3,4]` → `[2,4]`
- [ ] `$keys($my_obj)` where `my_obj: {b:2,a:1}` → `["b","a"]`
- [ ] `$values($my_obj)` where `my_obj: {b:2,a:1}` → `[2,1]`
- [ ] `$entries($my_obj)` where `my_obj: {b:2,a:1}` → `[{key:"b",value:2},{key:"a",value:1}]`
- [ ] `$from_entries($my_arr)` where `my_arr: [{key:"a",value:1}]` → `{a:1}`
- [ ] `$pick($my_obj, $keys)` where `my_obj: {a:1,b:2,c:3}` and `keys: ["a","c"]` → `{a:1,c:3}`
- [ ] `$omit($my_obj, $keys)` where `my_obj: {a:1,b:2,c:3}` and `keys: ["b"]` → `{a:1,c:3}`
- [ ] `$type($my_val)` where `my_val: "hello"` → `"string"`
- [ ] `$is_array($my_val)` where `my_val: [1,2]` → `true`
- [ ] `$is_object($my_val)` where `my_val: {a:1}` → `true`
- [ ] `$is_string($my_val)` where `my_val: "hello"` → `true`
- [ ] `$is_number($my_val)` where `my_val: 42` → `true`
- [ ] `$is_null($my_val)` where `my_val: null` → `true`
- [ ] `$to_string($my_val)` where `my_val: 123` → `"123"`
- [ ] `$to_number($my_str)` where `my_str: "3.14"` → `3.14`
- [ ] `$coalesce($a, $b, 1)` where `a: null` and `b: null` → `1`
- [ ] `$sum($my_arr)` where `my_arr: [1,2,3]` → `6`
- [ ] `$avg($my_arr)` where `my_arr: [1,2,3]` → `2.0`
- [ ] `$min($my_arr)` where `my_arr: [3,1,2]` → `1`
- [ ] `$max($my_arr)` where `my_arr: [3,1,2]` → `3`
- [ ] `$if($cond, $yes, $no)` where `cond: true`, `yes: "yes"`, `no: "no"` → `"yes"` (only `$yes` invoked, not `$no`)
- [ ] `$when($double, $nums)` where `double: "${0 * 2}"` and `nums: [1,2,3]` → `[2,4,6]`

### E011 type errors (all must fire)
- [ ] `$split(123, ",")` → E011 (first arg not string)
- [ ] `$split("a", 123)` → E011 (second arg not string)
- [ ] `$join("not an array", ",")` → E011
- [ ] `$trim(123)` → E011
- [ ] `$upper([])` → E011
- [ ] `$lower(123)` → E011
- [ ] `$replace(123, "a", "b")` → E011
- [ ] `$sort("not an array")` → E011
- [ ] `$reverse(123)` → E011
- [ ] `$unique("not array")` → E011
- [ ] `$flatten("not array")` → E011
- [ ] `$first("not array")` → E011
- [ ] `$last(123)` → E011
- [ ] `$slice(123, 0, 1)` → E011
- [ ] `$keys("not object")` → E011
- [ ] `$values(123)` → E011
- [ ] `$entries([])` → E011
- [ ] `$pick(123, [])` → E011
- [ ] `$omit(123, [])` → E011
- [ ] `$if("not bool", 1, 2)` → E011
- [ ] `$sum(["a","b"])` → E011
- [ ] `$avg(["a"])` → E011
- [ ] `$min(["a"])` → E011
- [ ] `$max([true])` → E011
