---
version: "1.14"
title: "Build-time & binary-size metrics"
short: "cargo build --timings report + ymx-cli size trend; optional rust-code-analysis complexity export"
description: |
  Capture build-time and binary-size metrics once the `ymx-cli` binary exists
  (milestone 1.10). Either extends `.github/workflows/ci.yml` or adds a
  dedicated `.github/workflows/metrics.yml` to: run
  `cargo build --workspace --timings` and post a per-crate timing breakdown to
  `$GITHUB_STEP_SUMMARY`, track `target/debug/ymx-cli` size (and a
  release-stripped variant) across builds as a `binary-size.json` artifact, and
  — optionally — export cyclomatic complexity via Mozilla's `rust-code-analysis`
  (PMD-style JSON) for trending on `main`. Informational-only initially; gating
  decisions deferred until a stable baseline exists.
status: planned
depends_on: ["1.10"]
tags: [ci, metrics, perf, infra]
---

# 1.14 — Build-time & binary-size metrics

## Tasks

- [x] Timing report
  - [x] In `ci.yml` (or new `metrics.yml`): `cargo build --workspace --timings`
  - [x] Upload `target/cargo-timings.html` as artifact `timings`
  - [x] Post a per-crate timing breakdown to `$GITHUB_STEP_SUMMARY` (parse the timings JSON or use a small script)
- [ ] Binary-size tracking
  - [ ] Capture `ls -l target/debug/ymx-cli` size; also build `--release` stripped and capture that size
  - [ ] Write both to `binary-size.json`; upload as artifact per build (no historical trend store until a remote / trend backend exists)
- [ ] (Optional) Cyclomatic-complexity export
  - [ ] Add `rust-code-analysis-action` to export PMD-style JSON as an artifact for trending on `main`
  - [ ] Decide whether to surface `metrics.yml` as a separate workflow vs. fold into `ci.yml`
- [ ] Decide gate posture (recommend informational-only initially); record a follow-up to set size/time budgets once a baseline is established

## Acceptance

- [ ] `cargo-timings.html` artifact downloadable; per-crate timing line in the run summary
- [ ] `binary-size.json` captured per build for `ymx-cli` (debug + release-stripped)
- [ ] (If taken) `rust-code-analysis` PMD JSON artifact present and parseable
- [ ] Metrics are informational-only (do not fail PRs) unless explicitly changed later