---
version: "1.18"
title: "Rule 18: `$` math shorthand property-key modifier"
short: "Trailing `$` on property keys as `${...}` shorthand"
description: |
  Implement the `$` property-key modifier (rule 18): `<name>$: <src>` is exactly
  equivalent to `<name>: ${<src>}`. The value must be a String math source.
  Also covers: `$` on integer keys (`0$:` ⇒ math source for slot `$0`), `$` on
  component/template names (`a$: x + y` ≡ `a: ${x + y}`), and combination
  `?$` (`<name>?$: <src>` ≡ `<name>?: ${<src}` — optional with math-evaluated
  default). Wrong order `$?:` is E010.
status: planned
depends_on: ["1.6"]
tags: [resolver, core, rules]
---

# 1.18 — Rule 18: `$` math shorthand property-key modifier

## Tasks

- [x] Strip `$` from key name; wrap value in `${...}` internally
- [x] `$` on integer keys (`0$:`) → math source evaluated for slot `$0`
- [x] `$` on component/template name (`a$: src`) → component output is math result
- [x] `?$` combination: `<name>?$: <src>` ≡ `<name>?: ${<src>}` — optional with math-evaluated default
- [x] Wrong modifier order `$?:` → E010; non-string `$`-suffix value → E010
- [x] `$` on meta fields (`_ymx`/`_test`) → E010

## Acceptance
- [x] Scenario `tests/cases/rule-18/math-shorthand/` passes
- [x] `cargo test --workspace` passes
- [x] `cargo fmt --check && cargo clippy --workspace --all-targets` passes clean
