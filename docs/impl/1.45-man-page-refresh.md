---
version: "1.45"
title: "Man page & docs refresh"
short: "Bring ymx.1 man page up to date with _use, rules 19–22, CLI flags, _ymx fields, diagnostics"
description: |
  The ymx.1 man page is significantly behind the project's feature set. This
  milestone brings it up to date by adding: _use section (three forms, transitive
  re-export, IPC declarations), rules 19–22 (shell builtins, HTML renderer, IPC
  via _use, brace calls), missing CLI flags (-c, --plain, --no-exec, --allowed-backends,
  --no-ipc, --allowed-ipc, --errors), _ymx front-matter reference section,
  diagnostic codes E016/E017/E018, and updated examples. No crate code changes.
status: done
depends_on: ["1.44"]
tags: [docs, man-page, cli]
---

# 1.45 — Man page & docs refresh

## Problem

The `ymx.1` man page (at `crates/ymx-cli/man/ymx.1`) was written for v1.0–v1.18
and has not been updated since. It is missing coverage of:

- `_use` directive (v1.25, v1.38) — the entire multi-file import system
- Rules 19–22 (v1.27, v1.35, v1.42–1.43, v1.39) — shell, HTML, IPC, brace calls
- CLI flags added after v1.18: `-c`, `--plain`, `--plain-template`, `--no-exec`,
  `--allowed-backends`, `--no-ipc`, `--allowed-ipc`, `--errors`
- `_ymx` front-matter fields: `allowed_backends`, `allowed_ipc`, `plain`
- Diagnostic codes E016, E017, E018
- Examples for multi-file projects, `-c`, shell commands, IPC, HTML

## Tasks

### Task 1: Add `_use` section to man page

- [x] Add a new `.SH "MULTI-FILE PROJECTS"` section between `.SH DESCRIPTION` and `.SH OPTIONS`
- [x] Document the three `_use` forms:
  - Bare `*` (recursive wildcard, default when no `_use` present)
  - Wildcard object `{"*": "file"}` (import all public components from file)
  - Named entries `{alias: "file.component"}` (import specific component under alias)
- [x] Document transitive re-export: imported file's own `_use` is processed first; imported names are re-exportable
- [x] Document file-scoped components (`_`-prefixed) cannot be imported (E005)
- [x] Document cycle detection (E001)
- [x] Document IPC declarations: mapping RHS (not string) = IPC component (rule 21)
- [x] Add examples for each form

### Task 2: Add rules 19–22 to man page

- [x] Add `.SS "Rule 19 — Shell components (sh, pw)"` under `.SH RULES`
  - `$sh{cmd}` / `$pw{cmd}` value expressions
  - Property-key shorthand: `main$sh: cmd`
  - `allowed_backends` restriction
  - Result shape: `{exit_code, stdout}`
  - E016 for execution errors
- [x] Add `.SS "Rule 20 — HTML renderer"`
  - `from` property for tag selection
  - `children` shape (scalar, object with `from`, null)
  - Literal tag syntax: `from: <name>`
  - Shorthand: `<name>: value`
  - `class`/`style` normalization
  - E017 for array/object children without `from`
- [x] Add `.SS "Rule 21 — External components via _use IPC declarations"`
  - Mapping RHS in `_use` = IPC component
  - Config reference tables (runner, transport, protocol, request shaping, response mapping, HTTP shaping, timeouts, lifecycle hooks)
  - Wire format: `mode: text` vs `mode: json`
  - Session lifetime
  - E018 for IPC call failures
- [x] Add `.SS "Rule 22 — Brace calls and the $<name> key suffix"`
  - `$name{payload}` syntax
  - Payload grammar (scalar, object, array, empty)
  - Key suffix: `key$name: value`
  - Equivalences with `from` and shortcut forms

### Task 3: Add missing CLI flags

- [x] Add `-c, --code <yml>` to `.SS "Compiler flags"` — inline YAML/JSON component definitions
- [x] Add `--plain` and `--plain-template` to `.SS "Compiler flags"` — namespace promotion
- [x] Add `--no-exec` and `--allowed-backends <list>` to a new `.SS "Execution and IPC"` subsection
- [x] Add `--no-ipc` and `--allowed-ipc <list>` to the same subsection
- [x] Add `--errors` to `.SS "Output and test"` — show errors only (no JSON on success)
- [x] Update the existing `--max-depth` description to mention brace calls and IPC calls count as recursive operations

### Task 4: Add `_ymx` front-matter reference

- [x] Add a new `.SH "PROJECT METADATA"` section with subsections:
  - `.SS "_ymx — front matter"` with a table of all fields: `max_depth`, `from_keyword`, `format`, `pretty`, `plain`, `allowed_backends`, `allowed_ipc`
  - Each field: type, default, notes
  - Precedence: CLI flag > entry-file `_ymx` > engine default
- [x] `.SS "_test — inline tests"` — brief description with forms A, B-value, B-error
- [x] `.SS "_use — file imports"` — brief cross-reference to the MULTI-FILE PROJECTS section

### Task 5: Update diagnostic codes table

- [x] Add `E016` row: compile stage, "Shell execution error (executor not provided, disallowed backend, or spawn failure)"
- [x] Add `E017` row: render stage, "Array/object children without a from component wrapper"
- [x] Add `E018` row: compile stage, "IPC call failure: no host provided, disallowed transport, spawn failure, crash, protocol error, timeout, error_pattern match, non-2xx HTTP"
- [x] Fix `E013` description to note that brace form (rule 22) accepts array/object payloads

### Task 6: Update examples section

- [x] Add example: multi-file project with `_use`
- [x] Add example: `_use` with named import and rename
- [x] Add example: inline code with `-c`
- [x] Add example: `-c` with file overlay
- [x] Add example: shell execution `$sh{echo hello}`
- [x] Add example: IPC declaration in `_use`
- [x] Add example: HTML rendering with literal tags
- [x] Remove the duplicate "Run tests recursively" example (lines 529–534 are identical to 525–528)

### Task 7: Update header and misc

- [x] Update `.TH` date to `2026-09-04`
- [x] Update version in `.TH` from `0.1.0` to `1.45`
- [x] Update `.SH NAME` line: remove "(v1)" — the project is now v1.x with all features
- [x] Update `.SH DESCRIPTION` to mention `_use` and multi-file projects
- [x] Add `.SH "SEE ALSO"` cross-reference to `ymx(5)` if it exists, or note the full spec at `docs/PRD/`

## Acceptance

- [x] All tasks above are checked off
- [x] The man page renders correctly: `man ./crates/ymx-cli/man/ymx.1`
- [x] No changes to any crate code — this is docs-only
