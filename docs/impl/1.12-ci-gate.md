---
version: "1.12"
title: "CI gate & dependency security"
short: "GitHub Actions fmt/clippy/test gate + cargo-deny audit; realizes the 1.1 CI placeholder"
description: |
  Production CI for the v1 metrics program. `.github/workflows/ci.yml` runs the
  same trio the per-milestone gatekeeper already runs — `cargo fmt --check`,
  `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`
  — plus `cargo build --workspace --timings`, with Rust toolchain caching
  (Swatinem/rust-cache) and JSON message-format artifact upload (clippy.json,
  test.json, the cargo-timings HTML) so failures and per-crate build times are
  inspectable from the Actions UI. `.github/workflows/audit.yml` runs
  `cargo-deny check` (advisories + licenses + bans, configured via `deny.toml`)
  on every push to `main` and on a daily schedule; this supersedes a bare
  `cargo-audit` advisories-only run. Realizes the CI placeholder task seeded in
  milestone 1.1, and is the platform the later metrics milestones (1.13, 1.14)
  build on. Depends on 1.1 so `Cargo.toml` / `Cargo.lock` / `rust-toolchain.toml`
  exist.
status: done
depends_on: ["1.1"]
tags: [ci, metrics, security, infra]
---

# 1.12 — CI gate & dependency security

## Tasks

- [ ] Create `.github/workflows/ci.yml`
  - [ ] `actions/checkout@v6`; `dtolnay/rust-toolchain@stable` with `components: rustfmt,clippy`; `Swatinem/rust-cache@v2`
  - [ ] `cargo fmt --check`
  - [ ] `cargo clippy --workspace --all-targets --message-format=json -- -D warnings` piped to `clippy.json`; upload as artifact `clippy-json`
  - [ ] `cargo test --workspace --message-format=json` piped to `test.json`; upload as artifact `test-json`
  - [ ] `cargo build --workspace --timings`; upload `target/cargo-timings.html` as artifact `timings`
  - [ ] Pin every third-party Action to a release tag (or commit SHA) before trusting in `main`
- [ ] Author `deny.toml` at repo root
  - [ ] `[advisories]` section (vulnerability policy: deny / warn; `unmaintained` policy decision recorded)
  - [ ] `[licenses]` section (allow-list consistent with workspace deps — Apache-2.0, MIT, etc.)
  - [ ] `[bans]` section (duplicate / wildcard policy)
- [ ] Create `.github/workflows/audit.yml`
  - [ ] `taiki-e/install-action@cargo-deny`; `cargo deny check`
  - [ ] Triggers: `push` to `main` plus a daily `schedule:` cron (e.g. `0 6 * * *`)
- [ ] Verify the gate mirrors gatekeeper's commands (`fmt --check`, `clippy --workspace --all-targets`, `test --workspace`) so CI and local `/build` verify stay aligned
- [ ] (Note) Do not edit milestone 1.1; this milestone supersedes its CI-placeholder task by realizing it. Optionally add a one-line pointer in 1.1's CI task if curation consistency calls for it — defer the decision and keep 1.1 untouched unless it reads ambiguously.

## Acceptance

- [ ] `ci.yml` runs green from a scratch checkout on a clean ubuntu-latest runner
- [ ] `audit.yml` runs `cargo deny check` clean
- [ ] `clippy-json`, `test-json`, `timings` artifacts are downloadable from a successful run
- [ ] CI gate and `/build` gatekeeper both pass the SAME `fmt`/`clippy`/`test` commands