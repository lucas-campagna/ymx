---
version: "1.11"
title: "Scenario suite & docs"
short: "tests/cases/rule-NN scenarios; README; examples"
description: |
  Author the integration scenario suite under `tests/cases/rule-NN/<scenario>/`
  — one directory per scenario, each a real YMX project with a `_test` block.
  Cover rules 1–16 (positive + diagnostic expectations for reachable codes),
  the entry-path model, namespace promotion (`plain`), file-scope, and edge
  cases. Add a top-level `README.md` and a couple of `examples/` projects.
status: done
depends_on: ["1.10"]
tags: [tests, scenarios, docs]
---

# 1.11 — Scenario suite & docs

## Tasks

- [ ] Per rule, scenario dirs `tests/cases/rule-NN/<scenario>/`
  - [ ] rule-01..16 with positive cases mirroring PRD examples
  - [ ] Diagnostic scenarios for reachable codes: E002, E003, E005, E006, E008, E010 (call-site/escape/math-id/mixed-shape — not the `_test`-malformed kind), E011, E012, E013, E009, and the unknown-`_ymx`-field part of E010
  - [ ] Load-time codes (E001/E004/E007/E015) covered by crate `#[test]` unit tests w/ inline YAML (NOT scenarios)
  - [ ] Entry-path scenarios: `main.main` default; `a.b.c`; ambiguous `.yml`/`.yaml` -> E009; 1-segment -> E009
  - [ ] `plain` scenarios: `false` (namespaced templates), `true` (promote all), `template` (promote templates only), promotion clash -> E004
  - [ ] File-scope cross-doc reference -> E005
  - [ ] `$reduce([])` -> Null; `$map`/`$reduce` scalar item -> `$0`
- [ ] Ensure every scenario defines >=1 `_test` entry; harness (1.9) asserts all pass
- [ ] `README.md` (root): what ymx is, install, CLI usage, link to docs/PRD.md
- [ ] `examples/` — 2–3 small projects (e.g. a `user`/`$box` template demo, a `$reduce` running total)
- [ ] `docs/impl/*` statuses updated to `done` as scenarios land

## Acceptance
- [ ] `cargo test --workspace` green; every scenario passes
- [ ] `cargo run -- ymx examples/<demo>` outputs sane JSON
- [ ] README + examples committed; PRD referenced