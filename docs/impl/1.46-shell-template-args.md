---
version: "1.46"
title: "IPC shell command template args"
short: "Append request_template to shell command string when shell: true"
description: |
  Fix IPC host to interpolate request_template into the shell command string
  when shell: true, instead of writing it to stdin. Shell commands like curl
  read arguments from argv, not stdin, so the template must be part of the -c
  string.
depends_on: ["1.43"]
status: done
tags: [ipc, bugfix, shell]
---

# 1.46 — IPC shell command template args

## Task 1: Modify build_command to accept request args (ymx-lib)

- [x] 1.1 Add optional `request_args: Option<(&IndexMap<String, Value>, &str)>` parameter to `build_command` (args + template string)
- [x] 1.2 When `shell: true` and template is provided, interpolate the template with args using `interpolate_template`
- [x] 1.3 Append the interpolated template to the shell command string before passing to `sh -c`
- [x] 1.4 Ensure the template string is properly escaped for shell interpretation (quote the interpolated string)

### Acceptance criteria

- [x] `shell: true` + `request_template` → template is part of the `-c` string, not stdin
- [x] `shell: false` (default) → behavior unchanged (template goes to stdin)
- [x] No template set → behavior unchanged

## Task 2: Modify spawn_session and call to pass args (ymx-lib)

- [x] 2.1 Add `request: Option<&IpcRequest>` parameter to `spawn_session`
- [x] 2.2 In `call()`, pass the request through to `spawn_session`
- [x] 2.3 Extract args + template from request and pass to `build_command`

### Acceptance criteria

- [x] `spawn_session` receives request args when available
- [x] `build_command` receives args + template when `shell: true` + template set

## Task 3: Modify do_protocol to skip stdin for shell+template (ymx-lib)

- [x] 3.1 When `shell: true` and template is set, skip the stdin write in `do_protocol`
- [x] 3.2 Still read stdout/stderr as normal for response capture

### Acceptance criteria

- [x] `shell: true` + template → no stdin write (template is in command)
- [x] `shell: false` or no template → stdin write unchanged

## Task 4: Fix examples/nc-curl/main.yml (code fix)

- [x] 4.1 Verify the example compiles and the `_test` block passes after the code changes

### Acceptance criteria

- [x] `ymx examples/nc-curl/main.yml --test` → PASS
- [x] `ymx examples/nc-curl/main.yml` → produces expected output

## Task 5: Test scenarios (tests/)

- [x] 5.1 Add scenario `tests/cases/rule-21/shell-template/` — `shell: true`, `cmd: echo`, `request_template: "$0"` → echo returns the arg
- [x] 5.2 Verify existing rule-21 test scenarios still pass (no regression)

### Acceptance criteria

- [x] New shell-template scenario passes
- [x] All existing rule-21 scenarios still pass
- [x] `cargo test --workspace` passes

## Acceptance

- [x] `cargo fmt --check` passes
- [x] `cargo clippy --workspace --all-targets` passes
- [x] `cargo test --workspace` passes
- [x] `examples/nc-curl/main.yml --test` passes
