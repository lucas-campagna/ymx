---
version: "1.1"
title: "Workspace & crate scaffolding"
short: "Cargo workspace, 6 crates, edition 2021, shared deps, rust-toolchain, CI"
description: |
  Stand up the Cargo workspace per PRD §Architecture: the 6 crates
  (ymx-core, ymx-config, ymx-test, ymx-lib, ymx-cli, ymx-web stub), edition
  2021, shared dependencies (serde_json with preserve_order via indexmap,
  yaml-rust2), and a rust-toolchain.toml pinning the MSRV. No real logic yet —
  only crate skeletons that compile and a workspace-level CI check.
status: planned
depends_on: []
tags: [scaffold, workspace]
---

# 1.1 — Workspace & crate scaffolding

## Tasks

- [ ] Create root `Cargo.toml` workspace manifest
  - [ ] `[workspace]` members = `crates/*`
  - [ ] `resolver = "2"`
  - [ ] `[workspace.package]` with `edition = "2021"`
  - [ ] Shared `[workspace.dependencies]`: `serde`, `serde_json` (features `preserve_order`), `indexmap`, `yaml-rust2`, `thiserror` (if used)
- [ ] `rust-toolchain.toml` pinning the toolchain channel to latest stable
- [ ] Crate `ymx-core` (lib)
  - [ ] `Cargo.toml`: dep `yaml-rust2`, `serde`, `serde_json` (preserve_order), `indexmap`
  - [ ] `lib.rs` with module stubs (`ir`, `diag`, `project`, `resolve`, `math`, `builtin`)
- [ ] Crate `ymx-config` (lib) — dep `ymx-core`
- [ ] Crate `ymx-test` (lib) — dep `ymx-core`
- [ ] Crate `ymx-lib` (lib) — dep `ymx-core` (re-export surface); `dev-dep` none
- [ ] Crate `ymx-cli` (bin) — dep `ymx-lib`, `ymx-config`, `ymx-test`
- [ ] Crate `ymx-web` (lib stub) — `lib.rs` `compile_error!("v3+ not in v1")`-style stub or empty
- [ ] `.gitignore` (target/, *.rs.bk)
- [ ] CI: GitHub Actions (or just local script) running `cargo fmt --check`, `cargo clippy --workspace --all-targets`, `cargo test --workspace`

## Acceptance

- [ ] `cargo build --workspace` succeeds with no warnings from stubs
- [ ] `cargo test --workspace` runs (0 tests, success)
- [ ] `cargo fmt --check` clean
- [ ] `ymx-web` stub compiles without pulling in HTTP deps