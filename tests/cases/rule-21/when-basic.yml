# tests/cases/rule-21/when-basic.yml
---
_test:
  main: {result: [2,4,6]}
  empty: {result: []}
  transform_all: {result: [10,0,-6]}

double: "${$0 * 2}"
nums: [1,2,3]
main: $when($double, ${nums()})
empty: $when($double, [])
transform_all: $when($double, [5, 0, -3])
