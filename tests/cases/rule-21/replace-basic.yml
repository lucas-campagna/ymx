# tests/cases/rule-21/replace-basic.yml
---
main: $replace("hello world", "world", "rust")
_test:
  main: "hello rust"
  all: "bbbb"
  none: "abc"

all: $replace("aaaa", "a", "b")
none: $replace("abc", "x", "y")
