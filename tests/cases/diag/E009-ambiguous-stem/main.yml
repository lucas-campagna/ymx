# E009: both main.yml and main.yaml exist — ambiguous stem.
# resolve_entry finds two candidates and returns E009 before checking components.
# The component name in the entry path (main.main) doesn't matter at this point.
helper: value
