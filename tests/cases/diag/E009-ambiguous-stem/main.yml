# E009: both main.yml and main.yaml exist — ambiguous stem.
# OLD BEHAVIOR: passing directory to load_project returned E009.
# NEW BEHAVIOR: load_project takes a file path. Explicit file wins.
# This scenario is now a no-op (no assertions).
helper: value
