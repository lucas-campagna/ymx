# E010: _ymx.plain is a bare bool (YAML unquoted), not a string.
# The valid values are the strings "false", "true", "template".
_ymx:
  plain: true
helper: value
_test:
  _build_error: E010
  helper:
    result: value
