# x?$: optional property with math default.
# When caller supplies x, default is skipped.
# When caller omits x, math default is evaluated.
comp_with_arg:
  from: comp
  x: 99
comp_no_arg:
  from: comp
comp:
  x?$: "2 + 3"
_test:
  comp_with_arg: {x: 99}
  comp_no_arg: {x: 5}
