# x?$: optional property with math-evaluated default.
# When caller supplies x, default is skipped.
# When caller omits x, math expression is evaluated lazily.
comp_with_arg:
  from: comp
  x: 99
comp_no_arg:
  from: comp
comp:
  x?$: "2 + 3"
_test:
  comp_with_arg: {x: 99}
  comp_no_arg: {x: 5}
