# Rule 20: from: <main> renders the HTML tag even when a "main" component exists.
# "main" (no angle brackets) calls the component; <main> renders the tag.
main: "from component"

tag_over_component:
  from: <main>
  children: from tag

component_call:
  from: main

_test:
  tag_over_component:
    format: html
    result: "<main>from tag</main>"
  component_call:
    result: "from component"
